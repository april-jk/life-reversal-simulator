# 人生逆转模拟器 — 开发规格文档（AGENTS.md）

> 本文档是「人生逆转模拟器」的完整设计与开发协议，供开发 Agent 直接据此实现。
> 所有核心决策已锁定，未经产品方确认不得变更标注为【已定】的条目。

---

## 1. 项目背景

- 场景：黑客松（主题框架：**reverse / 逆转**）。
- 产品名：**人生逆转模拟器**。
- 一句话概念：用户输入人生中正在面临的关键抉择，AI Agent 将每条选择路径往后推演成分支树（选择 → 困难 → 用户应对 → 后续局面），以思维导图形式呈现；用户可在任意节点点击 **reverse** 回溯，重新做出别的选择，推演另一条人生线。

---

## 2. 核心玩法【已定】

### 2.1 交互主循环（方案 B：交互推演）

1. **用户输入关键抉择**（如"我现在 20 岁，要不要辍学创业"），生成根节点（decision）。
2. Agent 生成 **多条选择分支**（branch，如"辍学创业" / "继续读完大学"，每次至多三个分支）。
3. 用户**点击**某条分支 → Agent **懒生成**该路径上的 **3 个困难**（difficulty）。用户也可以补充自己想到的困难。
4. 用户**点击某个困难**，并在该节点输入框中填写"**我会怎么处理**"（response）。
5. Agent 基于用户的应对，推演 **3 个后续局面**（situation）。
6. 推演最多**三级**（分支 = 第 1 级，困难 = 第 2 级，局面 = 第 3 级）。到达第 3 级后，Agent 生成**阶段性结局**（outcome），收口该条人生线。
7. 用户可点击任意历史节点上的 **reverse** 按钮，回溯到该节点做出别的选择，从该节点生长出新的分支。

### 2.2 分支规模

- 每层推演节点均至多生成 3 条，最多有 3 × 3 × 3 = 27 种可能，满树共 1 + 3 + 9 + 27 = **40 个主推演节点**。`response` 与 `outcome` 同样作为独立持久化记忆节点，不计入这个主推演节点规模。
- **懒生成（lazy generation）【已定】**：节点内容只在用户点击对应节点时才调用 Agent 生成，不做全树实时推演。每次点击 = 1 次 Agent 调用。
- 点击后前端**先渲染骨架节点（loading 态）**，Agent 返回后填充内容，避免用户干等。

### 2.3 reverse 语义【已定】

- 被 reverse 的旧子树**不删除**，整体**灰显保留**（半透明），让用户能看见"自己放弃的那条人生"。
- 从被回溯的节点处生长新分支，形成一条新链。
- **无剧透污染原则**：被回溯掉的子树内容，不得进入后续任何 Agent 调用的上下文。Agent 只"知道"从根节点到当前节点这条激活路径上发生过的事。

---

## 3. 困难生成规则【已定】

### 3.1 频率与烈度约束

- 只生成"**走这条路的大多数人真实会遇到的问题**"（高频问题）。
- 每次生成 3 个困难：**至少 2 个高频，最多允许 1 个中频**；不生成低频/黑天鹅事件。
- **禁止极端事件**：重病、牢狱、灭顶之灾、用户无法做任何应对的纯死局，一律不生成。困难必须是"正常决策可以应对"的级别。

### 3.2 合格困难的四条硬标准

1. **有因果**：能向前追溯到某个已发生的节点，显式写出因果链（如"经验欠缺 → 不敢在合同里争取预付款 → 垫资干活"），禁止无源之水。
2. **够具体**：带细节、带量级，不写空泛描述（"账上只剩两个月工资的钱"优于"现金流紧张"）。
3. **可应对**：用户必须能对它采取行动。
4. **符合档位**：高/中频标签必须如实，不允许夸大烈度。

### 3.3 双轴标签

- 每个困难节点携带两个标签：**probability（发生概率：high / medium）** 与 **impact（影响程度：low / medium）**。
- 概率与影响是两个独立维度，不得混为一谈。前端用颜色/图标区分展示。

### 3.4 用户自添困难

- 用户可在任意 branch 节点下补充自己想到的困难。
- 用户自添的困难与 Agent 生成的困难走**完全相同**的后续流程（点击 → 输入应对 → 推演局面），逻辑统一。

---

## 4. 时间系统【已定】

- **根节点时间锚点 = 用户首次提问的当天日期**（如 2026-07-25）。用户无需手动设置。
- **年龄是可选锚点**：用户在问题中说"我现在 20 岁"，就把 20 岁钉在根节点上；没说就没有年龄标签。
- **相对时间兜底【已定】**：Agent 生成节点内容时只写相对时间偏移（如"+6 个月""第二年"），前端结合根锚点自动换算显示（如"2027 年 1 月"）。
- 若用户在某节点补充了年龄/时间信息，以该节点为新锚点，对其下游节点自动重新换算对齐。
- 树层级本身隐含时间先后关系；Agent 生成时须保证下游节点的 `time_offset` 单调递增。

---

## 5. 数据模型

### 5.1 六种节点类型

| type | 触发方 | 说明 | 是否占深度 |
|---|---|---|---|
| `decision` | 用户 | 根节点：用户输入的关键抉择 | 否，depth 0 |
| `branch` | Agent | 选择分支（每次至多 3 条） | 第 1 级 |
| `difficulty` | Agent / 用户 | 该路径上的困难（每次 3 个） | 第 2 级 |
| `response` | 用户 | 用户对困难的应对（"我会怎么处理"） | **不占深度** |
| `situation` | Agent | 基于应对推演的后续局面（每次 3 个） | 第 3 级 |
| `outcome` | Agent | 阶段性结局，收口一条人生线 | 否，终点 |

> 注意：`response` 是交互动作，不是推演轮次，**不消耗三级深度额度**。

### 5.2 节点状态（status 枚举）

- `active`：正常节点。
- `pending`：骨架节点，Agent 正在生成中（loading 态）。
- `reversed`：被回溯的旧子树节点，灰显。

### 5.3 situation 的 trend 字段

- `better` / `worse` / `neutral`，表示用户应对之后局面的走向，前端据此着色。

---

## 6. JSON 结构协议【已定】

### 6.1 文件组织

```
/life-session-20260725-001/
  manifest.json          ← 唯一索引：整棵树结构、状态、reverse 历史
  nodes/
    n001.json            ← 每个节点一个文件：完整内容 + 路径记忆
    n002.json
    ...
```

- 运行时的真相来源是**内存中的树**；文件链负责持久化和 Agent 记忆。
- "每条分支的记忆是一串 JSON 文件"在**逻辑上**成立：链由 manifest 中的父子指针串成，而非按文件夹划分。原因：多条链共享祖先节点，按文件夹存会产生冗余和 reverse 时的不一致。
- 节点 ID 统一 `n001`、`n002`……递增编号。

### 6.2 manifest.json（结构索引）

```json
{
  "session_id": "life-20260725-001",
  "created_at": "2026-07-25T15:30:00+08:00",
  "root": {
    "node_id": "n001",
    "time_anchor": "2026-07-25",
    "age_anchor": 20
  },
  "nodes": [
    {
      "id": "n001",
      "parent": null,
      "type": "decision",
      "depth": 0,
      "status": "active",
      "title": "要不要辍学创业",
      "children": ["n002", "n003"],
      "file": "nodes/n001.json"
    },
    {
      "id": "n002",
      "parent": "n001",
      "type": "branch",
      "depth": 1,
      "status": "active",
      "title": "辍学创业",
      "time_label": "2026年9月",
      "children": ["n004", "n005", "n006"],
      "file": "nodes/n002.json"
    }
  ],
  "reverse_history": [
    {
      "at_node": "n005",
      "reversed_at": "2026-07-25T16:02:00+08:00",
      "retired_subtree_root": "n009",
      "new_chain_from": "n005"
    }
  ]
}
```

- 前端渲染思维导图**只读 manifest**，无需逐个打开节点文件。
- 每次 Agent 生成完成、用户输入 response、执行 reverse 后，同步更新 manifest。

### 6.3 节点文件（记忆机制的核心）

以困难节点为例：

```json
{
  "id": "n005",
  "type": "difficulty",
  "depth": 2,
  "status": "active",
  "source": "agent",
  "title": "客户回款慢，现金流紧张",
  "content": {
    "description": "初创团队没有议价能力，被迫接受 90 天账期，账上现金只够撑两个月工资。",
    "cause": "经验欠缺 → 不敢在合同里争取预付款 → 垫资干活",
    "frequency": "high",
    "impact": "medium",
    "time_offset": "+6个月",
    "time_label": "2027年1月"
  },
  "path_summary": [
    {"node": "n001", "type": "decision", "brief": "20岁，犹豫是否辍学创业"},
    {"node": "n002", "type": "branch", "brief": "选择辍学创业，接校园外包项目"},
    {"node": "n005", "type": "difficulty", "brief": "客户回款慢，现金流紧张"}
  ],
  "children": [],
  "agent_meta": {"call": "difficulty_gen", "generated_at": "2026-07-25T15:41:00+08:00"}
}
```

#### path_summary 传递规则【已定】

- 每个节点文件自带从根节点到自己的压缩叙事链。
- Agent 推演某节点的后续时，**只读取该节点这一个文件**，即获得全部合法上下文——天然实现"无剧透污染"。
- 生成子节点时：`子节点.path_summary = 父节点.path_summary + 父节点自身的一条 brief`。
- `brief` 为一句话概括（≤ 30 字），由生成方（Agent 或系统）写入。

#### 各类型节点的 content 字段

- **decision**：`{question, user_age?（用户提到的年龄，可空）}`
- **branch**：`{summary（路径概述）, time_offset, time_label}`
- **difficulty**：`{description, cause, frequency, impact, time_offset, time_label}`，`source` 为 `agent` 或 `user`
- **response**：`{text（用户输入的应对方式）}`
- **situation**：`{description, trend, time_offset, time_label}`
- **outcome**：`{summary（阶段性结语）, time_label}`

---

## 7. Agent 设计

### 7.1 四个独立调用

| 调用 | 触发时机 | 输入 | 输出（JSON） |
|---|---|---|---|
| 分支生成 `branch_gen` | 用户提交抉择 | decision + path_summary | `branches[]: {title, summary, time_offset}`，至多 3 条 |
| 困难生成 `difficulty_gen` | 用户点击 branch | branch 节点文件 | `difficulties[]: {title, description, cause, frequency, impact, time_offset}`，3 个（≥2 个 high，≤1 个 medium） |
| 应对推演 `response_gen` | 用户在 difficulty 节点提交应对 | difficulty 节点文件 + response 文本 | `situations[]: {title, description, trend, time_offset}`，3 个 |
| 结局生成 `outcome_gen` | situation 节点被激活且 depth=3 | situation 节点文件 | `outcome: {summary, time_label}`，1 段阶段性结语 |

### 7.2 Prompt 硬约束

- 困难生成必须遵守第 3 节全部规则（高频为主、最多一个中频、禁极端、四条合格标准），并要求模型**显式输出 `cause` 因果链**。
- 上下文只使用目标节点文件中的 `path_summary`，**禁止携带其他分支信息**。
- 时间只输出相对偏移（`time_offset`），绝对时间由系统换算。
- 深度控制：系统侧校验 depth，第 3 级 situation 之后只允许调用 `outcome_gen`。

### 7.3 输出校验【工程纪律】

- 所有 Agent 返回必须先过 **JSON Schema 校验**；不合法自动重试 1 次，仍失败则报错并在前端对应节点提示"推演失败，点击重试"。
- 建议附带一次**合理性自查**（模型自检生成内容与前文因果是否一致，不一致则重生成），可显著提升质量。

---

## 8. 前端要求

### 8.1 思维导图

- 推荐使用 **React Flow** 实现树形思维导图，从 manifest 渲染。
- 节点配色（建议）：decision 蓝、difficulty 红/橙、response 绿、situation 按 `trend` 上色（better 绿 / worse 红 / neutral 灰）、outcome 金。
- 节点上显示**时间徽章**（如"2027 年 1 月"），有年龄锚点时叠加年龄（如"21 岁 · 2027 年 1 月"）。
- difficulty 节点显示双轴标签（概率 / 影响）。
- 设计流畅的UI UX

### 8.2 交互细节

- **骨架节点**：点击后立刻画出 `pending` 态子节点，Agent 返回后填充。
- **difficulty 节点卡片必须内嵌输入框**（"你会怎么处理？"），用户提交后生成 response 子节点并触发应对推演。用户不输就不往下长。
- 每个节点悬停显示 **reverse 按钮**；点击后该节点旧子树整体**灰显保留**，从该节点长出新分支。
- outcome 节点上显示引导文案（如"想看看另一个选择的你吗？"），引导用户 reverse。

---

## 9. 演示保底策略（黑客松现场）

1. **预生成演示数据**：提前生成一条完整的"辍学创业"三级树（至多 40 个主推演节点，以及对应的 response / outcome 记忆 JSON），用于展示持久化结构。
2. **冷启动模板**：内置 3~4 个典型抉择（高考志愿 / 考研还是工作 / 要不要北漂 / 辍学创业），评委无需想输入即可体验。
3. **录屏备份**：提前录好完整操作流程视频。

---

## 10. 验收清单

- [ ] 用户输入抉择后，生成至多 3 条分支并正确渲染。
- [ ] 点击分支懒生成 3 个困难，困难符合第 3 节规则（频率、因果、双轴标签齐全）。
- [ ] 困难节点输入应对后，生成 3 个 situation（含 trend）。
- [ ] 第 3 级后自动生成 outcome，不再继续推演。
- [ ] reverse 后旧子树灰显保留，新分支从该节点生长，且旧子树内容不进入新分支的 Agent 上下文。
- [ ] 每个节点文件含完整 path_summary；manifest 与实际文件一致。
- [ ] 时间锚点默认为提问当天，相对时间正确换算；用户补年龄后下游正确对齐。
- [ ] 所有 Agent 输出经 JSON Schema 校验，失败有重试与前端提示。
